你这个报错 **`event not found`** 是因为 **bash 把 `!` 当成“历史命令展开”(history expansion)** 了，所以你的密码里有 `!` 时，shell 先处理了它，`mysql` 根本没拿到正确的密码。

✅ 最推荐的做法：**不要把密码写在命令里**，只用 `-p`，让它提示你输入密码（输入时不会回显）。

```bash
mysql -h mysql8.cknhhbopgucq.us-east-1.rds.amazonaws.com -P 3306 -u admin -p
```

然后它会显示：

```text
Enter password:
```

这时候再粘贴你的密码 `QVQYPCrc442#!Z5` 回车就行。

---

如果你一定要一行写完（不推荐），要用 **单引号** 包住并用 `-p<password>`（`-p` 后面不要空格）：

```bash
mysql -h mysql8.cknhhbopgucq.us-east-1.rds.amazonaws.com -P 3306 -u admin -p'QVQYPCrc442#!Z5'
```

（单引号能保护 `!` 不被 bash 展开；你之前用的“中文引号”也会出问题。）

---

**额外提醒（安全）**

* 命令行里直接带密码会被：

  * `history`
  * `ps` 进程列表
  * 终端记录
    看到。尽量用第一种交互输入。

---

如果你按第一种做了还是连不上，把报错贴出来（比如 Access denied / timeout），我再帮你定位是 **密码/权限** 还是 **Security Group/VPC** 问题。


**fault tolerance** 的中文翻译是：

👉 **容错性** 或 **容错能力**

---

### 技术语境里的常见解释

* **容错性**：
  系统在**部分组件发生故障**时，仍然能够**继续运行或快速恢复**的能力。

---

### 常见搭配翻译

* **high fault tolerance** → **高容错性**
* **fault-tolerant system** → **容错系统**
* **improve fault tolerance** → **提高容错能力**

---

### 一句话面试版

> **Fault tolerance refers to a system’s ability to continue operating despite failures.**


