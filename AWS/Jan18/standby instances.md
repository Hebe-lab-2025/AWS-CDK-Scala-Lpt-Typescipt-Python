**standby instances 的翻译与解释（AWS 高频）**

### 翻译

* **备用实例**
* **待命实例**
* **热备实例（语境相关）**

---

### AWS 中的常见含义

#### 1️⃣ **RDS Multi-AZ 的 Standby Instance（最常见）**

* **含义**：RDS 在另一个可用区（AZ）中自动维护的**备用数据库实例**
* **特点**：

  * ❌ 不对外提供读写
  * ❌ 不能直接访问
  * ✅ 自动同步数据
  * ✅ 主实例故障时自动切换（Failover）

👉 用于 **高可用（HA）**，不是扩展性能

---

#### 2️⃣ **Auto Scaling 的 Standby（较少见）**

* 实例被放入 **Standby 状态**
* 不接收流量，但仍保留
* 常用于维护或排错

---

### 一句话面试总结

> **Standby instances are pre-provisioned backup instances that take over automatically during failures to ensure high availability.**

---

### 易混点（面试陷阱）

* ❌ Standby ≠ Read Replica
* ❌ Standby ≠ 负载均衡
* ✅ Standby = **高可用保障**

如果你愿意，我可以帮你出 **5 道 RDS Multi-AZ vs Read Replica 必错题**，非常适合 SAA-C03。


这是 **AWS 面试里最容易混的点之一**，我直接给你一个**对照表 + 一句话结论**。

---

## Backup vs Standby（核心区别）

| 对比项   | **Backup（备份）** | **Standby（备用实例）** |
| ----- | -------------- | ----------------- |
| 作用    | **数据恢复**       | **高可用 / 自动接管**    |
| 是否实时  | ❌ 否（时间点）       | ✅ 是（持续同步）         |
| 是否运行  | ❌ 不运行          | ✅ 运行中             |
| 是否可访问 | ❌ 不可直接访问       | ❌（RDS 中）          |
| 故障切换  | ❌ 手动恢复         | ✅ 自动 failover     |
| 恢复时间  | 慢（分钟~小时）       | 快（秒~分钟）           |

---

## 在 AWS 里的具体例子

### 🗄️ Backup（备份）

* RDS Automated Backup
* RDS Snapshot
* EBS Snapshot
* S3 Versioning / Backup

👉 **用途**：

> 删错数据 / 数据损坏 / 回滚到某个时间点

---

### 🔁 Standby（备用）

* **RDS Multi-AZ Standby Instance**

👉 **用途**：

> 主实例挂了，**立刻顶上**

---

## 一句话“死记版”（面试必背）

> **Backup is for data recovery; Standby is for high availability.**

---

## 再给你一个记忆钩子 🧠

* **Backup = 存起来**
* **Standby = 跑着等**

---

## 考试陷阱提醒

❗ **Backup ≠ High Availability**
❗ **Standby ≠ Scaling**

如果你愿意，我可以下一步直接给你 **5 道 Backup vs Standby 真题陷阱**（SAA-C03 风格）。


**DB instance identifier 的翻译与解释（RDS 高频）**

### 翻译

* **数据库实例标识符**
* **数据库实例名称**

---

### 是什么？

**DB instance identifier** 是你在创建 **RDS 数据库实例**时指定的：

* 一个 **唯一名称**
* 用来 **识别这一个 RDS 实例**
* 类似于资源的“名字”，不是数据库名

---

### 重要特性

* **在同一 AWS 区域内唯一**
* 只能包含：
  `字母 + 数字 + 连字符 (-)`
* **必须以字母开头**
* 创建后 **不能修改**（删除再建）

---

### 常见易混点（必考）

| 概念                  | 是不是 DB instance identifier |
| ------------------- | -------------------------- |
| 数据库名（database name） | ❌                          |
| 主机名 / Endpoint      | ❌                          |
| ARN                 | ❌                          |
| RDS 实例名称            | ✅                          |

---

### 例子

* `prod-mysql-01`
* `rds-orders-db`
* `test-db-instance`

---

### 一句话面试版

> **The DB instance identifier is a unique name used to identify
> and manage an RDS instance within a region.**

如果你愿意，我可以帮你整理一张 **RDS 创建时 10 个字段“哪些能改、哪些不能改”速查表**。


**翻译：**

在 **“Instance configuration”**（实例配置）部分，
选择 **“Burstable classes（包含 t 系列）”**，然后选择 **`db.t4g.micro`** 选项。

In the "Instance configuration" section, 
select "Burstable classes (includes t classes)" and then select "db.t4g.micro" option.


