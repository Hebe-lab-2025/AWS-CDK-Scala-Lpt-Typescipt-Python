chmod 400 usercode/RDS-client-key-pair

这条命令的意思很简单，但**非常关键**👇

---

## `chmod 400 usercode/RDS-client-key-pair.pem`

### 翻译

> **把 `RDS-client-key-pair.pem` 的权限设置为：只有文件所有者可读。**

---

### 权限 `400` 是什么意思？

| 数字    | 含义            |
| ----- | ------------- |
| **4** | 可读（read）      |
| **0** | 不可写（write）    |
| **0** | 不可执行（execute） |

👉 **只有你自己能读这个私钥文件**，其他人完全没权限。

---

### 为什么必须这么做？

因为 **SSH 对私钥权限有强制要求**：

* 如果权限太宽（如 644、600 以上）
* SSH 会直接拒绝：

  ```
  Permissions 0644 for 'xxx.pem' are too open.
  ```

---

### 在 RDS 场景中的真实含义

* `.pem` 用于 **SSH 登录 EC2（Bastion Host）**
* **不是** 用来直接连 RDS
* RDS 通过 **EC2 中转** 访问

---

### 一句话面试版

> **chmod 400 ensures that the SSH private key is readable only by the owner, 
which is required for secure SSH access.**

如果你愿意，我可以下一步帮你讲清：
👉 **为什么 AWS 要强制私钥权限这么严（安全设计角度）**


