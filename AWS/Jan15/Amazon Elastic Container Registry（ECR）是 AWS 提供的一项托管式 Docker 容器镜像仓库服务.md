**翻译：**

> **Lambda 擅长处理事件驱动、短生命周期的任务，但并不适合长时间运行或有状态的应用。**
> Lambda excels at event-driven, short-lived tasks but may not be suitable for long-running or stateful applications.


**翻译：**

> **Amazon Elastic Container Registry（ECR）是 AWS 提供的一项托管式 Docker 容器镜像仓库服务。
> ECR 提供安全、可扩展的仓库，用于存储、管理和部署 Docker 镜像。**
>
> Amazon Elastic Container Registry (ECR) is an AWS managed Docker container registry service.
> ECR provides a secure and scalable repository to store, manage, and deploy Docker images.




