下面是这套 **65 题模拟卷的完整答案 + 解析**，
每题都按你要求：✅ 先英文解释，✅ 再中文解释。
（题目我用简短描述帮你回忆场景）

---

## 📑 Answer Sheet (1–65, with explanations)

---

### 1️⃣ Private EC2 needs internet but not public

**Answer:** B – NAT Gateway

**English explanation:**
Use a NAT Gateway in a public subnet so instances in private subnets can initiate outbound internet access without being directly reachable from the internet.

**中文解释：**
在公有子网放 NAT 网关，让私有子网里的 EC2 可以 **主动出网**，但没有公网 IP，外部不能直接访问。

---

### 2️⃣ Private access to AWS services without Internet

**Answer:** B – PrivateLink

**English explanation:**
AWS PrivateLink exposes services through interface VPC endpoints using private IP addresses, avoiding the public internet.

**中文解释：**
PrivateLink 通过 **接口型 Endpoint** 用私有 IP 暴露服务，整个访问流量不走公网。

---

### 3️⃣ Access S3 from private subnet without NAT cost

**Answer:** C – S3 Gateway Endpoint

**English explanation:**
An S3 Gateway Endpoint gives private connectivity from your VPC to S3 without using NAT or internet, and it’s free.

**中文解释：**
S3 网关端点可以让 VPC 私网直接访问 S3，既不用 NAT，又不会产生公网费用。

---

### 4️⃣ Connect hundreds of VPCs centrally

**Answer:** B – Transit Gateway

**English explanation:**
Transit Gateway acts as a hub, simplifying many-to-many VPC connections into hub-and-spoke.

**中文解释：**
Transit Gateway 像网络“总线”，用 **中心辐射** 模型代替大量 VPC 互相 Peering 的网状结构。

---

### 5️⃣ Security Group property

**Answer:** B – Stateful

**English explanation:**
Security groups are stateful: if traffic is allowed in one direction, the return traffic is automatically allowed.

**中文解释：**
Security Group 是 **有状态** 的，允许入站后，返回流量会自动放行。

---

### 6️⃣ NACL characteristics

**Answer:** A, B, C, D

**English explanation:**
NACLs are stateless, evaluated in order, apply at subnet level, and support explicit allow and deny rules.

**中文解释：**
NACL 是 **无状态** 的，
规则有顺序，作用在 **子网级别**，可以显式允许或拒绝。

---

### 7️⃣ Private DNS for VPC endpoint not working

**Answer:** Private DNS / DNS resolution not enabled

**English explanation:**
If VPC DNS resolution or private DNS for the endpoint is disabled, instance cannot resolve the service’s private DNS name.

**中文解释：**
如果 VPC 的 DNS 解析或 Endpoint 的 **私有 DNS** 没开启，EC2 就无法解析这个私网域名。

---

### 8️⃣ Two VPCs (different accounts) need private connection

**Answer:** VPC Peering (for small scale)

**English explanation:**
VPC peering is designed for private connectivity between two VPCs, even across accounts, without using the internet.

**中文解释：**
VPC Peering 支持 **跨账号 VPC 私网互连**，适合 VPC 数量不多的场景。

---

### 9️⃣ On-premises to AWS via dedicated physical link

**Answer:** Direct Connect

**English explanation:**
Direct Connect provides dedicated, private connectivity with consistent performance between your data center and AWS.

**中文解释：**
Direct Connect 是一条到 AWS 的 **专线连接**，比 VPN 更稳定、更低延迟。

---

### 🔟 Use VPN as failover to Direct Connect

**Answer:** Direct Connect + VPN failover via BGP

**English explanation:**
Use a VPN over the internet as backup and BGP routing to automatically fail over if Direct Connect goes down.

**中文解释：**
通常做法是：主路 Direct Connect，备路 VPN，通过 BGP 自动在专线故障时切换到 VPN。

---

### 1️⃣1️⃣ Lambda in private subnet can’t reach Internet

**Answer:** No NAT Gateway configured

**English explanation:**
Lambda functions in private subnets have no public IP and need a NAT Gateway to access the internet.

**中文解释：**
挂在私有子网的 Lambda 没有公网 IP，如果路由没有指向 NAT，就无法访问互联网。

---

### 1️⃣2️⃣ Need static outbound IP for calling third-party APIs

**Answer:** NAT Gateway with Elastic IP

**English explanation:**
All outbound traffic from private instances is translated to the NAT Gateway’s Elastic IP, which you can whitelist with the third party.

**中文解释：**
通过 NAT Gateway，私网流量会统一从它绑定的 **EIP** 出去，方便第三方做白名单。

---

### 1️⃣3️⃣ On-prem DNS queries to Route 53

**Answer:** Route 53 Resolver inbound endpoint

**English explanation:**
Inbound endpoints let on-prem DNS resolvers send DNS queries into Route 53 Resolver for VPC-hosted names.

**中文解释：**
创建 **Route 53 Resolver Inbound Endpoint** 后，本地 DNS 才能把解析请求发到 VPC 里来解析私网域名。

---

### 1️⃣4️⃣ Multi-region private connectivity between many VPCs

**Answer:** Transit Gateway inter-Region peering

**English explanation:**
Transit Gateway peering across Regions gives scalable private connectivity between multiple regional hubs.

**中文解释：**
通过跨区域 TGW Peering，可以在多个 Region 的 TGW 之间建立 **高吞吐的私网互联**。

---

### 1️⃣5️⃣ Prevent S3 data exfiltration via VPC endpoints

**Answer:** VPC Endpoint policy restricting target buckets

**English explanation:**
Use an endpoint policy so traffic from the VPC endpoint can only access specific S3 bucket ARNs.

**中文解释：**
在 Endpoint Policy 里限制只允许访问指定的 S3 Bucket ARN，防止流量被导出到别的桶。

---

## ⚡ Serverless (Q16–Q30)

---

### 1️⃣6️⃣ Lambda max execution time

**Answer:** 15 minutes

**English explanation:**
Lambda supports a maximum timeout of 900 seconds per invocation.

**中文解释：**
Lambda 每次调用最长运行 **15 分钟**，超出需要换别的方案。

---

### 1️⃣7️⃣ Avoid duplicate order creation with retries

**Answer:** Use idempotency token / idempotent design

**English explanation:**
Include an idempotency key so multiple submissions with the same key only create one order.

**中文解释：**
通过 **幂等 ID**，让同一个请求重复到达时只执行一次下单逻辑，避免重复数据。

---

### 1️⃣8️⃣ Lambda exhausts DB connections

**Answer:** Use RDS Proxy

**English explanation:**
RDS Proxy manages a shared pool of database connections for many Lambda invocations.

**中文解释：**
RDS Proxy 负责维护数据库连接池，Lambda 不直接连库，避免高并发把连接打满。

---

### 1️⃣9️⃣ 4-hour ETL batch job

**Answer:** Step Functions + ECS/Fargate

**English explanation:**
The workload exceeds Lambda’s limit and needs long-running containers orchestrated by Step Functions.

**中文解释：**
4 小时的 ETL 超出 Lambda 限制，适合用 **Fargate 容器**，再用 Step Functions 做流程编排和重试。

---

### 2️⃣0️⃣ Millions of IoT events per second

**Answer:** Kinesis Data Streams

**English explanation:**
Kinesis Data Streams is built for high-throughput, ordered event ingestion with reprocessing support.

**中文解释：**
Kinesis 可以高吞吐、有序地接收海量事件，并支持回放与多消费者，是 IoT 场景常用方案。

---

### 2️⃣1️⃣ Event-driven image resize pipeline

**Answer:** S3 Event → Lambda

**English explanation:**
Configure S3 object-created events to trigger a Lambda function to generate thumbnails.

**中文解释：**
S3 在对象上传成功后触发事件，由 Lambda 接到事件后生成缩略图或处理图片。

---

### 2️⃣2️⃣ Keep Lambda warm for low latency

**Answer:** Provisioned Concurrency

**English explanation:**
Provisioned concurrency keeps a number of Lambda execution environments initialized and ready.

**中文解释：**
预置并发会让一定数量的 Lambda 实例 **常驻内存**，减少冷启动带来的延迟抖动。

---

### 2️⃣3️⃣ Orchestrate multiple Lambda steps and branches

**Answer:** Step Functions

**English explanation:**
Step Functions coordinate multiple Lambda functions with states, retries, and branching logic.

**中文解释：**
Step Functions 是 **工作流服务**，负责任务的顺序、分支、重试等逻辑，Lambda 专心写业务代码。

---

### 2️⃣4️⃣ Serverless relational database

**Answer:** Aurora Serverless v2

**English explanation:**
Aurora Serverless v2 scales capacity automatically and charges per second, ideal for spiky workloads.

**中文解释：**
Aurora Serverless v2 会根据负载自动放大/缩小容量，秒级计费，适合负载不稳定的关系型数据库场景。

---

### 2️⃣5️⃣ Serverless containers

**Answer:** AWS Fargate

**English explanation:**
Fargate runs containers without managing EC2 instances or clusters.

**中文解释：**
Fargate 让你只关心容器镜像和资源配置，不用管底层 EC2 节点，是无服务器容器方案。

---

### 2️⃣6️⃣ Queue-based asynchronous processing

**Answer:** SQS + Lambda

**English explanation:**
SQS stores work items reliably, and Lambda scales automatically to process messages from the queue.

**中文解释：**
SQS 做 **可靠排队**，Lambda 则自动伸缩去消费队列里的任务，非常适合解耦异步后台处理。

---

### 2️⃣7️⃣ Broadcast notifications to many subscribers

**Answer:** SNS Topic

**English explanation:**
SNS publishes a message to a topic which then fans out to multiple endpoints such as email, Lambda, or SQS.

**中文解释：**
SNS Topic 是 **发布订阅模型**，一条消息可以同时推送到多个终端（邮件、SQS、Lambda 等）。

---

### 2️⃣8️⃣ Need event replay and ordered records

**Answer:** Kinesis Data Streams

**English explanation:**
Kinesis stores records for a retention period, allowing consumers to re-read and replay data as needed.

**中文解释：**
Kinesis 会把数据保留一段时间，消费者可以按分片和序号重复读取，实现 **回放** 和 **顺序消费**。

---

### 2️⃣9️⃣ Can cold starts be completely removed?

**Answer:** No – only reduced/mitigated

**English explanation:**
Cold starts cannot be fully eliminated; they can only be reduced using techniques like provisioned concurrency.

**中文解释：**
冷启动无法彻底消除，只能通过 **预置并发、合理内存配置** 等手段尽量减少和稳定延迟。

---

### 3️⃣0️⃣ Best Lambda workload pattern

**Answer:** Bursty, intermittent, event-driven, short-lived jobs

**English explanation:**
Lambda is optimal when workloads have idle periods and don’t justify always-on servers.

**中文解释：**
Lambda 最适合那种 **间歇性、事件驱动、执行时间短** 的任务，不值得为它一直开 EC2。

---

## 🗄 Storage & S3 (Q31–Q40)

---

### 3️⃣1️⃣ 500 TB, infrequent access, millisecond retrieval

**Answer:** S3 Glacier Instant Retrieval

**English explanation:**
Glacier Instant Retrieval is designed for rarely accessed data that must still be retrieved in milliseconds at low cost.

**中文解释：**
Glacier Instant Retrieval 专门为 **低频访问但要求毫秒级取回** 的冷数据设计，成本比 Standard 更低。

---

### 3️⃣2️⃣ Archive where hours of retrieval time is okay

**Answer:** S3 Glacier Flexible Retrieval

**English explanation:**
Flexible Retrieval offers lower storage cost with retrieval times from minutes to hours.

**中文解释：**
Flexible Retrieval 存储费用更低，但取回需要 **分钟到小时**，适合不急着读的数据。

---

### 3️⃣3️⃣ Cheapest archive, can wait days to restore

**Answer:** S3 Glacier Deep Archive

**English explanation:**
Deep Archive is the lowest-cost storage class with retrieval times in hours (often used for compliance).

**中文解释：**
Deep Archive 是 S3 最便宜的档案级存储，恢复通常需要数小时，非常适合只做合规归档的数据。

---

### 3️⃣4️⃣ Global static website with HTTPS and caching

**Answer:** CloudFront + S3 + ACM

**English explanation:**
S3 hosts static content, CloudFront caches it globally, and ACM provides TLS certificates.

**中文解释：**
S3 放静态资源，CloudFront 在全球边缘缓存，ACM 免费签发 HTTPS 证书，这是托管静态网站的经典组合。

---

### 3️⃣5️⃣ Prevent accidental deletion of objects

**Answer:** Versioning + MFA Delete

**English explanation:**
S3 versioning keeps previous versions, and MFA Delete protects against accidental or malicious deletion.

**中文解释：**
开启版本控制后，即使删除也能恢复；再加上 **MFA Delete** 防止误删或恶意删除。

---

### 3️⃣6️⃣ Cross-account secure access to S3 bucket

**Answer:** IAM role + STS AssumeRole

**English explanation:**
The external account assumes an IAM role that has permission to access the bucket, avoiding long-term keys.

**中文解释：**
通过跨账号角色（AssumeRole）授权访问 S3，不需要共享长期 access key，更安全。

---

### 3️⃣7️⃣ Block any public access to the bucket

**Answer:** S3 Block Public Access

**English explanation:**
Block Public Access settings override ACLs and bucket policies to prevent public exposure.

**中文解释：**
“Block Public Access” 会统一阻止 Bucket 被设成公开，即使 ACL 或策略错误配置也能挡住。

---

### 3️⃣8️⃣ S3 read-after-write consistency?

**Answer:** Yes, strong read-after-write for all operations now

**English explanation:**
S3 now provides strong consistency for PUT and DELETE of objects in all Regions.

**中文解释：**
现在的 S3 对对象的新增和覆盖 **全区域都支持强一致性读写**，创建后立刻可读。

---

### 3️⃣9️⃣ Best storage layer for a data lake

**Answer:** S3

**English explanation:**
S3 provides durable, scalable, low-cost storage and integrates with many analytics services.

**中文解释：**
数据湖的底层存储一般选 S3：
便宜、持久、几乎无限扩展，并且可以和 Glue、Athena、EMR 等工具联动。

---

### 4️⃣0️⃣ Lifecycle to IA at 30 days, Glacier at 180 days

**Answer:** Valid and cost-effective design

**English explanation:**
Moving older data to colder tiers automatically reduces costs while preserving availability needs.

**中文解释：**
通过生命周期策略将 30 天后的数据转到 IA，再在 180 天后转到 Glacier，是典型的 **自动分层降本方案**。

---

## 🔢 Databases (Q41–Q50)

---

### 4️⃣1️⃣ High read latency on RDS

**Answer:** Add Read Replicas

**English explanation:**
Read replicas offload read traffic from the primary instance.

**中文解释：**
增加只读副本，把大量读请求从主库上分流出去，降低读延迟。

---

### 4️⃣2️⃣ Need automatic failover for RDS

**Answer:** Multi-AZ deployment

**English explanation:**
Multi-AZ creates a synchronous standby that takes over automatically on failure.

**中文解释：**
Multi-AZ 会创建一个同步的备库，主库出故障时自动切换，实现高可用。

---

### 4️⃣3️⃣ Global low-latency reads for Aurora

**Answer:** Aurora Global Database

**English explanation:**
It replicates data to secondary Regions with low-latency read endpoints.

**中文解释：**
Aurora Global Database 可以把数据复制到其他 Region，并在本地提供低延迟只读节点。

---

### 4️⃣4️⃣ Millions of TPS key-value, single-digit ms latency

**Answer:** DynamoDB

**English explanation:**
DynamoDB is a fully managed NoSQL key-value store built for massive scale and low latency.

**中文解释：**
DynamoDB 是全托管的 NoSQL 键值数据库，特点就是 **高吞吐 + 低延迟 + 自动扩缩容**。

---

### 4️⃣5️⃣ Hot partition issue in DynamoDB

**Answer:** Randomize or shard partition key

**English explanation:**
Use a more evenly distributed partition key (e.g., add random suffix) to avoid concentrating traffic on one partition.

**中文解释：**
通过 **打散分区键**（比如加随机后缀），让请求更均匀地落到不同分区，避免热点分区。

---

### 4️⃣6️⃣ Need transactions and SQL

**Answer:** RDS / Aurora

**English explanation:**
Relational engines like RDS or Aurora provide SQL and full ACID transactions.

**中文解释：**
要 SQL + 事务，一般就选 **RDS 或 Aurora** 这类关系型数据库。

---

### 4️⃣7️⃣ Auto-scaling relational database

**Answer:** Aurora Serverless v2

**English explanation:**
It can automatically scale up or down capacity based on demand.

**中文解释：**
Aurora Serverless v2 会根据负载自动调整实例容量，不需要自己手动改规格。

---

### 4️⃣8️⃣ DynamoDB costs too high, data very rarely accessed

**Answer:** Use DynamoDB Standard-IA table class

**English explanation:**
Standard-IA offers lower storage costs for infrequent access patterns.

**中文解释：**
切换到 **Standard-IA 表类型**，用更低的存储成本来存久不访问的数据。

---

### 4️⃣9️⃣ Reduce read load on DB

**Answer:** ElastiCache (Redis)

**English explanation:**
Caching popular data in Redis offloads reads and improves latency.

**中文解释：**
把热点数据缓存到 Redis（ElastiCache），可以显著减少数据库读请求、提升响应速度。

---

### 5️⃣0️⃣ Migrate live database with minimal downtime

**Answer:** Database Migration Service (DMS)

**English explanation:**
DMS supports continuous replication and cutover with minimal downtime.

**中文解释：**
DMS 支持 **持续增量同步**，可以在短暂停机窗口内完成切换，减少业务中断时间。

---

## 🔐 Security / IAM / KMS (Q51–Q60)

---

### 5️⃣1️⃣ Avoid long-term cross-account credentials

**Answer:** STS AssumeRole

**English explanation:**
Use STS to obtain temporary credentials by assuming a role in the other account.

**中文解释：**
通过 STS AssumeRole 在对方账号里扮演一个角色，拿到 **短期凭证**，避免共享长期密钥。

---

### 5️⃣2️⃣ Automatic KMS CMK rotation period

**Answer:** 365 days

**English explanation:**
KMS supports automatic annual rotation for customer-managed keys.

**中文解释：**
KMS 可以对自管密钥每隔 **365 天自动轮换**，符合很多合规要求。

---

### 5️⃣3️⃣ Encrypt EBS volume

**Answer:** Use EBS encryption with a KMS CMK

**English explanation:**
Enable EBS encryption using a KMS key to protect data at rest.

**中文解释：**
在 EBS 上启用加密，并指定一个 KMS 密钥，就可以保护磁盘上静态数据。

---

### 5️⃣4️⃣ Detect exposed IAM keys on GitHub

**Answer:** GuardDuty (or Security Hub with GuardDuty findings)

**English explanation:**
GuardDuty can detect unusual API calls and potential credential leaks.

**中文解释：**
GuardDuty 能检测异常 API 使用行为，比如凭证疑似泄露时发出告警。

---

### 5️⃣5️⃣ Protect root account from misuse

**Answer:** Enable MFA and avoid daily use

**English explanation:**
Attach MFA to the root account and only use it for critical operations; use IAM users/roles for normal tasks.

**中文解释：**
给 root 账号开启 **多因素认证**，平时不用 root，只在极少数关键操作时才用。

---

### 5️⃣6️⃣ Need fine-grained row/column-level security for data lake

**Answer:** Use Lake Formation / catalog-level permissions, not just S3

**English explanation:**
S3 controls are object-level; row/column-level control requires data lake services like Lake Formation with integrated permissions.

**中文解释：**
S3 只做到对象级权限，要细到 **行/列级**，需要用 Lake Formation 等数据湖权限管理，而不是只靠 S3 Bucket Policy。

---

### 5️⃣7️⃣ Web and mobile app federated login

**Answer:** Amazon Cognito

**English explanation:**
Cognito supports user pools, identity pools, and federation with external IdPs for apps.

**中文解释：**
Cognito 提供用户池、身份池，并且可以和外部 IdP（Google、Facebook 等）对接，是做应用登录的标准服务。

---

### 5️⃣8️⃣ Temporary access for contractors

**Answer:** IAM role with limited session duration and policies

**English explanation:**
Create an IAM role with restricted permissions and let contractors assume it with temporary sessions.

**中文解释：**
给外包创建一个 **权限受限的角色**，通过 STS 临时扮演这个角色，过期就自动失效。

---

### 5️⃣9️⃣ Policy design best practice

**Answer:** Least privilege

**English explanation:**
Grant only the minimal permissions required to perform specific tasks.

**中文解释：**
**最小权限原则**：只给完成工作所必需的那一点权限，不多给。

---

### 6️⃣0️⃣ Audit all API calls in your AWS account

**Answer:** AWS CloudTrail

**English explanation:**
CloudTrail records API activity across services for auditing and compliance.

**中文解释：**
CloudTrail 会记录账号里几乎所有 API 调用日志，用于审计和安全排查。

---

## 📊 Monitoring & Cost Optimization (Q61–Q65)

---

### 6️⃣1️⃣ Identify idle or underutilized EC2

**Answer:** Cost Explorer Rightsizing (or Compute Optimizer)

**English explanation:**
Rightsizing recommendations show instances with low utilization and potential savings.

**中文解释：**
Cost Explorer 的 **Rightsizing 建议** 会标出利用率很低、可以降配或关停的 EC2。

---

### 6️⃣2️⃣ Set budget and alert when cost exceeds threshold

**Answer:** AWS Budgets

**English explanation:**
AWS Budgets lets you define cost or usage thresholds and sends notifications when they’re approached or exceeded.

**中文解释：**
AWS Budgets 可以设置费用或用量的阈值，并在接近或超过时发邮件或 SNS 告警。

---

### 6️⃣3️⃣ Commit to 1–3 years of compute usage to save money

**Answer:** Savings Plans

**English explanation:**
Savings Plans provide flexible pricing discounts in exchange for a commitment to a certain spend per hour.

**中文解释：**
Savings Plans 通过承诺一定的 **按小时消费额度** 来换取更低的计算费率，比按需便宜很多。

---

### 6️⃣4️⃣ Improve global performance and reduce origin load

**Answer:** Amazon CloudFront

**English explanation:**
CloudFront caches content at edge locations, reducing latency and load on the origin.

**中文解释：**
CloudFront 在全球边缘节点缓存内容，用户访问就近获取，既降低延迟又减轻源站压力。

---

### 6️⃣5️⃣ Identify high-level cost, security, and fault tolerance issues

**Answer:** AWS Trusted Advisor

**English explanation:**
Trusted Advisor analyzes your environment against AWS best practices and suggests improvements.

**中文解释：**
Trusted Advisor 会根据 AWS 最佳实践检查你的账号配置，在成本、安全、容错等方面给出优化建议。

---

如果你想，我们可以把这 65 题：

* 再按 **考点分类做错题本**，或者
* 抽 20 题做一轮 **口语面试模拟（你答我纠正）**，或者
* 整理成一页 **英文+中文的高频考点速记卡**。
