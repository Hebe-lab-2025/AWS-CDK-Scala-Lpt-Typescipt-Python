太好了，我**直接四个一起给你**，按 **SAA-C03 考试最常用形态**来 👇
（全是**秒选型**、**考场可直接用**）

---

## 一、Compute vs Storage vs Database（考试对照表）

| 维度      | Compute（算力）        | Storage（存储）    | Database（数据库）      |
| ------- | ------------------ | -------------- | ------------------ |
| 核心作用    | **运行代码**           | **存文件**        | **存结构化数据 + 查询**    |
| 是否执行逻辑  | ✅ 是                | ❌ 否            | ❌ 否                |
| 常见服务    | EC2 / Lambda / ECS | S3 / EBS / EFS | RDS / DynamoDB     |
| 典型关键词   | run code, process  | object, file   | query, transaction |
| 是否可横向扩展 | ✅                  | ✅              | ⚠️ 看服务             |
| 考试陷阱    | 把 EC2 当存储          | 把 S3 当数据库      | 把 RDS 当算力          |

**一句话记忆**

> **Compute = 干活的**
> **Storage = 放东西的**
> **Database = 查数据的**

---

## 二、10 道 Compute 场景题（秒选型）

1️⃣ **短任务 + 事件触发 + 不想管服务器**
👉 **Lambda**

2️⃣ **长期运行 Web 服务**
👉 **EC2**

3️⃣ **需要自动扩缩容 + HTTP 请求**
👉 **EC2 + ASG + ALB**

4️⃣ **Docker 容器 + 不想管集群**
👉 **ECS Fargate**

5️⃣ **Docker + 高度自定义网络/调度**
👉 **EKS**

6️⃣ **秒级并发 + 成本敏感**
👉 **Lambda**

7️⃣ **CPU 密集型任务**
👉 **EC2 Compute Optimized**

8️⃣ **内存密集型任务**
👉 **EC2 Memory Optimized**

9️⃣ **任务持续 30 分钟以上**
👉 **EC2 / ECS（不是 Lambda）**

🔟 **不可预测流量 Web 应用**
👉 **ALB + ASG + EC2**

---

## 三、Compute + ALB + Auto Scaling（ASCII 架构图）

```
        Users
          |
       (HTTPS)
          |
      ┌────────┐
      │  ALB   │  ← Load Balancing
      └────────┘
          |
   ┌───────────────┐
   │ Auto Scaling  │  ← Scale in / out
   └───────────────┘
      |        |
   ┌─────┐  ┌─────┐
   │ EC2 │  │ EC2 │  ← Compute
   └─────┘  └─────┘
```

**考试关键词对照**

* **ALB** → distribute traffic
* **ASG** → elasticity / high availability
* **EC2** → compute workload

---

## 四、SAA-C03 Compute 速记卡（中 + 英）

### 🧠 Compute 速记 1

* 中文：**短任务 / 事件驱动 / 不管服务器**
* English: **Serverless, event-driven**
* 👉 **Lambda**

---

### 🧠 Compute 速记 2

* 中文：**Web 应用 / 高可用 / 自动扩容**
* English: **Highly available web app**
* 👉 **ALB + ASG + EC2**

---

### 🧠 Compute 速记 3

* 中文：**容器 / 不想管底层**
* English: **Container without cluster management**
* 👉 **ECS Fargate**

---

### 🧠 Compute 速记 4

* 中文：**完全控制 OS**
* English: **Full control over OS**
* 👉 **EC2**

---

### 🧠 Compute 速记 5

* 中文：**成本敏感 + 不稳定流量**
* English: **Cost-efficient, unpredictable traffic**
* 👉 **Lambda**

---

## 接下来我还能继续帮你 👇

你可以直接选 **一个编号**：

1️⃣ 出 **Compute vs Database 高频陷阱题 15 道**
2️⃣ 把 **所有 Compute 服务做成一页考试脑图**
3️⃣ 来一套 **SAA-C03 真题风格 Compute 决策树**
4️⃣ 帮你做一张 **“看到关键词 → 秒选服务” 对照表**

你说 **选哪个**，我立刻继续。
