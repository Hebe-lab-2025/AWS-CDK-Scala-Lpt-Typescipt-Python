太好了，这一组正是 **SAA-C03 最容易被“混着考”的核心区**。
我按你给的顺序：**图 → 陷阱题 → 升级对比 → 总复盘**，一次性打通 👇

---

## 🔹 一、Multi-AZ vs Read Replica（ASCII 对比图）

### ✅ RDS **Multi-AZ**（高可用 / 容灾）

```
        App
         |
      Write/Read
         |
     ┌────────┐
     │ Primary│  ← Active
     └────────┘
         ||
   (sync replication)
         ||
     ┌────────┐
     │ Standby│  ← Passive (no reads)
     └────────┘
       (Different AZ)

❗ Failover 自动
❗ Standby 不处理流量
```

---

### ✅ **Read Replica**（读性能 / 扩展）

```
        App
         |
       Writes
         |
     ┌────────┐
     │ Primary│
     └────────┘
        |   \
   (async)   (async)
        |     \
   ┌────────┐ ┌────────┐
   │ Replica│ │ Replica│  ← Read only
   └────────┘ └────────┘
     (AZ/Region)

❌ 不自动 failover
✅ Offload read traffic
```

---

### 🧠 一眼秒记

> **Multi-AZ = 活着不挂**
> **Read Replica = 读得更快**

---

## 🔹 二、5 道「HA vs 性能」混合陷阱题（真题风）

### 1️⃣

**数据库宕机必须自动切换，但读不多**
👉 **Multi-AZ**
❌ Read Replica（不会切）

---

### 2️⃣

**“offload read traffic from the primary database”**
👉 **Read Replica**
❌ Multi-AZ（Standby 不接读）

---

### 3️⃣

**既要自动 failover，又要处理大量读请求**
👉 **Multi-AZ + Read Replicas**
🧠 两个一起上（真考点）

---

### 4️⃣

**提升数据库 availability，不能改应用代码**
👉 **Multi-AZ**
❌ Aurora（迁移成本更高）

---

### 5️⃣

**读请求暴涨，主库 CPU 飙高**
👉 **Read Replica**
❌ Vertical scaling（贵 + 慢）

---

## 🔹 三、Aurora vs RDS（考试升级版）

### 📊 高阶对比表（考场用）

| 维度       | RDS             | Aurora             |
| -------- | --------------- | ------------------ |
| 架构       | 单实例 + 存储        | **分离式存储层**         |
| 副本       | 手动 Read Replica | **自动 6 副本 / 3 AZ** |
| Failover | 分钟级             | **秒级**             |
| 性能       | 好               | **更好（x3~x5）**      |
| 扩展       | 有上限             | **近乎无限**           |
| 成本       | 便宜              | 较贵                 |
| 迁移难度     | 低               | 中                  |

---

### 🧠 升级版考试判断法

* 题目只说 **HA / DR**
  👉 **RDS Multi-AZ**

* 题目说 **cloud-native / fastest recovery / massive scale**
  👉 **Aurora**

* 题目强调 **no code change / keep MySQL**
  👉 **RDS**

---

### ❗ 常见陷阱

> **Aurora 不是 Read Replica 的替代**
> **Aurora = 自带 HA + 性能**

---

## 🔹 四、三层架构（Web + App + DB）完整复盘

### 🏗️ 标准 SAA-C03 三层架构（ASCII）

```
              Users
                |
             (HTTPS)
                |
            ┌────────┐
            │  ALB   │   ← Web Tier
            └────────┘
                |
        ┌────────────────┐
        │ App Tier        │
        │ EC2 / ECS / ASG│  ← Compute
        └────────────────┘
                |
        ┌────────────────┐
        │ Database Tier   │
        │ RDS / Aurora   │  ← Data
        └────────────────┘
```

---

### 🧠 把“数据库选择”塞进三层架构

#### 情况 A：**高可用 Web 应用**

* Web：ALB
* App：ASG + EC2
* DB：**RDS Multi-AZ**

#### 情况 B：**读多写少**

* DB：**RDS + Read Replicas**

#### 情况 C：**高并发 + 高性能**

* DB：**Aurora**

---

### 📌 考场一句话模板（万能）

> A highly available three-tier architecture using an **Application Load Balancer**, an **Auto Scaling group** for the application tier, and a **Multi-AZ RDS / Aurora database** for resilience.

---

## 🧠 最终「闭眼决策口诀」

```
宕机会不会影响？
   |
  是
   |
 Multi-AZ

读是不是很多？
   |
  是
   |
Read Replica

又快又稳又大？
   |
  是
   |
Aurora
```

---

## 接下来我可以继续帮你 👇

你可以直接选：

1️⃣ **10 道 Aurora vs RDS “二选一”极限混淆题**
2️⃣ **把三层架构 + 解耦 + HA 做成一张终极决策脑图**
3️⃣ **SAA-C03 全套数据库 + 消息 + LB 联合模拟题**

你说 **继续哪个**，我马上开刷。
