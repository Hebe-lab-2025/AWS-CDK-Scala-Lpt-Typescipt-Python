太好了 👍
我直接把 **3 种版本都给你拆好**，你可以 **原样拷走就用**（GitHub / Notion / 考前一页纸）。

---

# ✅ 版本一：**GitHub 速记卡（README.md）**

> 适合：放在 repo 里，面试 / 考试前快速扫

```md
# AWS Exam Rapid Triggers (SAA-C03)

## Network
- Public vs Private Subnet → Route Table
- Private subnet outbound → NAT Gateway
- Instance-level firewall → Security Group (Stateful)
- Subnet-level firewall → NACL (Stateless)

## Compute
- Unpredictable traffic → ALB + Auto Scaling
- Stateless web tier → ALB + ASG
- Long-running task → EC2 / ECS
- Minimal ops → Lambda

## Database
- HA relational DB → RDS Multi-AZ
- Read-heavy → Read Replica
- Sub-ms latency → ElastiCache
- DynamoDB fast read → DAX

## Storage
- Object storage → S3
- Block storage → EBS
- Shared filesystem → EFS

## Decoupling
- Async processing → SQS
- Fan-out → SNS
```

📌 **用法**：

* 面试前 `git clone`
* 考前 5 分钟只看这个文件

---

# ✅ 版本二：**Notion 模板（直接可粘）**

> 适合：长期复习 + 条件反射训练

### 📕 AWS Exam Trigger Dictionary

#### 🌐 Network

* **private subnet outbound only** → NAT Gateway
* **public vs private subnet** → Route Table
* **instance-level firewall** → Security Group
* **subnet-level firewall** → NACL

---

#### ⚙️ Compute

* **unpredictable traffic** → Auto Scaling
* **stateless application** → ALB + ASG
* **no server management** → Lambda
* **long-running job** → EC2 / ECS

---

#### 🗄️ Database

* **high availability** → RDS Multi-AZ
* **read performance issue** → Read Replica / Cache
* **sub-millisecond latency** → ElastiCache
* **DynamoDB acceleration** → DAX

---

#### 🪤 Exam Traps

* EC2 alone ≠ scalable
* Multi-AZ ≠ performance
* Public IP ≠ Public Subnet
* NAT ≠ inbound traffic

📌 **Notion 用法建议**

* Toggle 折叠 = 考前速刷
* 每个 Trigger 自己补 1 道错题

---

# ✅ 版本三：**考前一页纸（打印 / 手机锁屏）**

```
AWS EXAM ONE-PAGER

Scalable / Unpredictable
→ ALB + Auto Scaling

Private subnet outbound
→ NAT Gateway

Public vs Private
→ Route Table (NOT Public IP)

Security
- SG = Stateful (Allow only)
- NACL = Stateless (Allow/Deny)

Database
- HA → RDS Multi-AZ
- Performance → Read Replica / Cache
- Sub-ms → ElastiCache
- DynamoDB accel → DAX

Storage
- Object → S3
- Block → EBS
- Shared → EFS

Traps
- EC2 alone ≠ scalable
- Multi-AZ ≠ faster
- Private ≠ no Internet
```

📌 **用法**

* 考前最后 10 分钟
* 地铁 / 电梯 / 等候区扫一遍

---

# 🎯 我可以继续帮你做的（任选）

1️⃣ **把你做错的题 → 自动归类到这些暗号下**
2️⃣ **只考 Trap 的 30 题模拟卷**
3️⃣ **把这套内容做成 Anki 闪卡**
4️⃣ **按你弱项定制一页纸（只留你容易错的）**

你直接告诉我：
👉 **GitHub / Notion / 一页纸** 想先用哪一个？
我可以 **再帮你精简到极致版**。
