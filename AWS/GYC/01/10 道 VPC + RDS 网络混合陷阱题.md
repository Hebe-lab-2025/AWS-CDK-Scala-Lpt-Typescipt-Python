好，**这是 SAA-C03 网络域里最容易丢分、但最容易秒杀的一组内容**。
我给你做成 **「看暗号 → 直接选」** 版本。

---

# 🔥 ① 10 道 **VPC + RDS 网络混合陷阱题（秒杀型）**

> 规则：
> **只抓网络暗号，不想数据库参数**

---

### Q1

题干：RDS 在 **private subnet**，应用在 EC2，**无法连接数据库**
✅ **检查 Security Group inbound 3306**
🔑 暗号：*private subnet ≠ 不可访问*

---

### Q2

题干：RDS 需要 **被公网客户端访问**
✅ **Public subnet + Publicly Accessible = YES**
🔑 暗号：*是否暴露公网*

---

### Q3

题干：EC2 在 private subnet，需要访问 **RDS（同 VPC）**
✅ **不需要 NAT Gateway**
🔑 暗号：*同 VPC 内通信*

---

### Q4

题干：RDS 在 private subnet，EC2 无法连，但 **ping 通**
✅ **检查 RDS Security Group**
🔑 暗号：*ping ≠ DB 端口*

---

### Q5

题干：RDS Multi-AZ，主库故障后应用连接失败
✅ **使用 RDS endpoint，不要写死 IP**
🔑 暗号：*failover + endpoint*

---

### Q6

题干：私有子网 EC2 无法 **下载补丁 / yum install**
✅ **需要 NAT Gateway（在 public subnet）**
🔑 暗号：*outbound internet*

---

### Q7

题干：数据库连接 timeout，刚加了 NACL
✅ **检查 NACL inbound + outbound**
🔑 暗号：*NACL 是 stateless*

---

### Q8

题干：多个应用访问 RDS，想限制来源
✅ **用 Security Group 引用 SG**
🔑 暗号：*SG to SG*

---

### Q9

题干：RDS 不需要公网访问，但需要高安全
✅ **Private subnet + no public IP**
🔑 暗号：*best practice*

---

### Q10

题干：EC2 在 public subnet 仍无法访问 RDS
✅ **检查 SG 是否允许 EC2 SG**
🔑 暗号：*subnet 不决定访问，SG 决定*

---

## 🧠 一句话总口诀（必背）

```
能不能连 RDS = Security Group
能不能上网 = NAT Gateway
Failover 不断连 = 用 endpoint
```

---

# 🧭 ② **SAA-C03 网络 & 数据库联合决策树（终极版）**

```text
【EC2 要访问 RDS】
        |
   是否同一 VPC？
        |
      YES
        |
  ------------------
  |                |
检查 SG        检查 NACL
(3306?)      (stateless)

【EC2 要访问 Internet】
        |
   是否 private subnet？
        |
      YES
        |
  NAT Gateway (public subnet)

【RDS 是否对公网开放？】
        |
   Publicly Accessible ?
      |        |
     YES      NO
      |        |
 Public subnet  Private subnet

【RDS Failover】
        |
  使用 Endpoint
 (不要 IP)
```

---

## 🧠 考试时你脑中要走的 **固定顺序**

1. **同 VPC？** → 不需要 NAT
2. **连不上 DB？** → 先看 SG
3. **能 ping 不能连？** → 端口问题
4. **私网 EC2 上不了网？** → NAT
5. **Multi-AZ 切换失败？** → endpoint

---

# 📄 ③ **VPC / NAT / SG / RDS 一页 Cheat Sheet（考试版）**

## 🔹 VPC / Subnet

* **Public subnet**：有 route 到 IGW
* **Private subnet**：没有 IGW route
* Subnet **不控制访问权限**

---

## 🔹 Security Group（最重要）

* **Stateful**
* 控制 **谁能连谁**
* RDS 最常见错误点
* 支持 **SG 引用 SG**

---

## 🔹 NACL

* **Stateless**
* 必须同时允许 inbound + outbound
* 很少用，但一加就容易错

---

## 🔹 NAT Gateway

* 放在 **public subnet**
* 作用：
  👉 private subnet **访问 Internet（仅 outbound）**
* ❌ 不用于访问 RDS

---

## 🔹 RDS 网络要点

* 建议：**Private subnet**
* 公网访问：`Publicly Accessible = YES`
* Multi-AZ：
  👉 **Failover 自动，但要用 endpoint**
* SG 决定一切

---

## ❌ 高频误区（必看）

| 错误想法                | 正确理解 |
| ------------------- | ---- |
| Private subnet 不能访问 | ❌    |
| 访问 RDS 需要 NAT       | ❌    |
| Ping 通就能连 DB        | ❌    |
| Subnet 决定安全         | ❌    |

---

# ⚡ 30 秒网络秒杀流程（背下来）

```text
连不上 DB？
→ SG
→ NACL
→ Endpoint

上不了网？
→ Private subnet
→ NAT Gateway

Failover 出问题？
→ 用 endpoint
```

---

## 🎯 考试生存法则（非常关键）

* **90% 网络题，答案是 Security Group**
* **NAT 只解决“上网”，不解决“互连”**
* **RDS 几乎永远在 private subnet**
* **Subnet ≠ 防火墙**

---

### 下一步我强烈建议你继续👇

* 🔥 **10 道 VPC 网络“反直觉挖坑题”**
* 🧪 **RDS 连接失败全场景排查模拟**
* 📄 **Networking Domain PDF（考前 10 分钟专刷）**

你直接回我 **A / B / C**，我立刻继续。
