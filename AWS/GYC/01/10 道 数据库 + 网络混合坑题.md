太好了，这一组我给你 **“数据库 × 网络”考场级工具包**：
👉 **先踩坑 → 再给三步排查 → 最后一整套 65 题数据库模拟卷**（全按 SAA-C03 口径）

---

# 🔥 一、10 道 **数据库 + 网络混合坑题**（高频误选）

> 规则：**每题抓 1 个网络点 + 1 个数据库点**

---

### Q1

**RDS 在 private subnet，EC2 连接超时**
✅ **检查 RDS Security Group inbound**
❌ NAT Gateway
🔑 坑点：连 DB ≠ 上网

---

### Q2

**RDS Multi-AZ 后 failover，应用报错**
✅ **使用 RDS endpoint（不是 IP）**
❌ 提高实例规格
🔑 坑点：网络没问题，是连接方式错

---

### Q3

**Private EC2 无法访问 RDS，但 ping 通**
✅ **端口 3306/5432 未放行（SG/NACL）**
❌ 子网类型
🔑 坑点：ping ≠ TCP 端口

---

### Q4

**Private EC2 访问 S3 读写数据，NAT 费用暴涨**
✅ **S3 Gateway Endpoint**
❌ 加大 NAT
🔑 坑点：AWS 服务 ≠ NAT

---

### Q5

**跨 AZ 的 EC2 访问 RDS 延迟高**
✅ **正常现象（跨 AZ）**
❌ 再加 Read Replica
🔑 坑点：不是网络故障

---

### Q6

**RDS 需要公网访问做运维**
✅ **Publicly Accessible + Public Subnet + SG**
❌ NAT
🔑 坑点：NAT 不支持入站

---

### Q7

**DB CPU 100%，Web 层已自动扩展**
✅ **Cache / Read Replica**
❌ 再加 ASG
🔑 坑点：瓶颈在数据层

---

### Q8

**Private subnet EC2 无法 yum install，DB 正常**
✅ **NAT Gateway**
❌ Endpoint（除非访问 AWS 服务）
🔑 坑点：访问公网

---

### Q9

**多个应用访问同一 RDS，想限制来源**
✅ **DB SG 引用 App SG**
❌ NACL
🔑 坑点：精确控制用 SG

---

### Q10（反直觉）

**小型 MySQL 应用，要求高可用**
✅ **RDS Multi-AZ**
❌ Aurora
🔑 坑点：HA ≠ 高性能

---

## 🧠 一句话总心法

```
连不上 DB → 先看 SG
要上网 → 才看 NAT
要 AWS 服务 → Endpoint
HA ≠ 性能
```

---

# 🧭 二、**数据库题「三步排查速记卡」**（考场必背）

```text
【Step 1｜网络能不能到？】
- 同 VPC？ → YES 默认可达
- 不同 VPC？ → Peering / TGW
- Public / Private 无关 DB 互连

【Step 2｜权限放没放？】
- Security Group（90% 问题）
  - DB SG 是否允许 App SG？
  - 端口对吗？
- NACL（最近是否改过？双向放行）

【Step 3｜数据库自身能力？】
- HA？ → Multi-AZ
- 读多？ → Read Replica
- 延迟？ → Cache
- Failover？ → 用 Endpoint
```

### ⚡ 30 秒排查口诀

```
先 SG
再路由
最后才想数据库选型
```

---

# 🧪 三、**SAA-C03 数据库完整模拟卷（65 题）**

> 使用建议：
>
> * 第 1 遍：**3 秒/题秒选**
> * 第 2 遍：只看 ❌ 陷阱点
> * 错题进「错题本模板」

---

## 🟦 A. 基础选型（1–20）

1. **Relational DB, HA required** → **RDS Multi-AZ**
2. **Read-heavy workload** → **Read Replica**
3. **Sub-millisecond latency** → **ElastiCache**
4. **MySQL-compatible, high performance** → **Aurora**
5. **Small app, HA only** → **RDS Multi-AZ**
6. **Analytics slowing production DB** → **Read Replica**
7. **Hot keys causing DB load** → **Cache**
8. **Cross-Region DR (read)** → **Aurora Global DB**
9. **NoSQL key-value, massive scale** → **DynamoDB**
10. **Ultra-fast DynamoDB reads** → **DAX**

---

## 🟦 B. HA / 性能陷阱（21–35）

21. **Multi-AZ improves performance** → ❌
22. **Read Replica provides failover** → ❌
23. **Cache replaces DB** → ❌
24. **Failover < 30s** → **Multi-AZ / Aurora**
25. **Latency-sensitive reads** → **Cache**
26. **Scale reads without app change** → **Read Replica**
27. **Write-heavy workload** → ❌（SAA 少考，别乱选）
28. **DB CPU 100% during reads** → **Cache**
29. **HA + low latency** → **Multi-AZ + Cache**
30. **Reporting queries** → **Read Replica**

---

## 🟦 C. 数据库 × 网络（36–55）

36. **RDS in private subnet, EC2 can’t connect** → **Check SG**
37. **Failover breaks connection** → **Use Endpoint**
38. **Private EC2 access S3** → **Gateway Endpoint**
39. **Private EC2 access external API** → **NAT**
40. **Block specific IP to DB** → **NACL**
41. **Only app servers access DB** → **SG-to-SG**
42. **Cross-VPC DB access** → **VPC Peering / TGW**
43. **Ping works, DB timeout** → **Port/SG**
44. **Publicly accessible DB** → **Public Subnet + Flag + SG**
45. **Reduce NAT cost for AWS APIs** → **Endpoint**

---

## 🟦 D. 反直觉 & 成本（56–65）

56. **Aurora for HA small app** → ❌
57. **NAT for S3 best practice** → ❌
58. **Private subnet = cannot access DB** → ❌
59. **Endpoint can access internet** → ❌
60. **Read Replica = HA** → ❌
61. **Cache solves write bottleneck** → ❌
62. **Multi-AZ = read scaling** → ❌
63. **DB failover requires app change** → ❌（用 endpoint）
64. **Security Group is stateless** → ❌
65. **Subnet controls security** → ❌

---

## 🎯 数据库终极 3 行背诵

```
HA → Multi-AZ
读多 → Read Replica
延迟 → Cache
```

---

## 📘 附：错题本模板（强烈建议用）

```text
【题号】
Q37

【关键词】
private subnet, cannot connect, RDS

【我选的】
NAT Gateway

【误选原因】
把“连 DB”当成“上网问题”

【正确秒杀规则】
连 DB → Security Group
```

---

如果你愿意，下一步我可以继续帮你：

* 🔥 **数据库 × 网络 × 成本 30 题地狱卷**
* 📄 **把三步排查卡 + 65 题做成 PDF**
* 🧠 **数据库暗号 → 服务 → 架构 Anki 闪卡**

你直接回我 **A / B / C** 或 **“全都要”**。
