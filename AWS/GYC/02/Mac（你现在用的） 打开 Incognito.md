很简单，我按 **你现在用的设备 + 最快方式** 给你讲。

---

## ✅ 在 **Mac（你现在用的）** 打开 Incognito（无痕窗口）

### 🌐 **Chrome（最常见）**

**快捷键（最快）**：

```text
⌘ Command + ⇧ Shift + N
```

**菜单方式**：

1. 右上角「⋮」
2. 选择 **New Incognito Window（新建无痕窗口）**

👉 你会看到：

* 黑色窗口
* 提示 “You’ve gone incognito”

---

### 🌐 **Safari**

**快捷键**：

```text
⌘ Command + ⇧ Shift + N
```

**菜单方式**：

1. 菜单栏 **File**
2. **New Private Window**

👉 地址栏会变成深色

---

### 🌐 **Firefox**

**快捷键**：

```text
⌘ Command + ⇧ Shift + P
```

---

## 🔐 为什么 AWS 一定要你用 Incognito？

一句话解释你刚看到的 Note：

> **AWS 不允许在同一个浏览器 session 里同时登录两个 federated 身份（比如 root / IAM / Lab user）**

如果你不用 Incognito：

* Cookie / Session 冲突
* AWS 会直接 **block login**
* 常见报错：`AccessDenied` / 自动跳回原账号

**Incognito = 全新 cookie + 全新 session**

---

## 🧪 推荐你现在的操作顺序（不踩坑版）

1. **当前窗口别关**（保留你现在的 AWS 登录）
2. `⌘ + ⇧ + N` → 打开无痕窗口
3. 在无痕窗口里：

   * 打开 AWS 登录链接
   * 登录 **IAMLabUser**
4. 两个账号可以并行用，互不影响

---

## 🧠 面试 / 考试暗号（顺手记一句）

> *“Use an incognito window to avoid federated session conflicts.”*

---

如果你愿意，我也可以帮你解释：

* **federated session 是什么**
* **IAM user vs role vs federated login**
* **为什么 AWS 实验环境特别容易踩这个坑**

你现在是用 **Chrome 还是 Safari**？
